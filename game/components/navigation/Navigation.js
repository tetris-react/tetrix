import React from 'react';
import { NavContainer } from '../../styles';

const Navigation = () => {
  return (
    <NavContainer>
      <div />
    </NavContainer>
  );
};

export default Navigation;
